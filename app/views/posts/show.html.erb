<p id="notice"><%= notice %></p>

<div>
  <strong>Name:</strong>
  <% if @post.user %>
  <%= @post.user.name %>
  <% end %>
</div>

<p>
  <strong>Body:</strong>
  <pre><%= @post.body %></pre>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
